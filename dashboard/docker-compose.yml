version: '3.8'

services:
  dashboard-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard-frontend
    ports:
      - "4321:3000"
    restart: unless-stopped
    env_file:
      - .env
